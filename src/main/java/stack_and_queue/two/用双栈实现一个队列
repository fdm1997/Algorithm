用两个栈实现队列的功能，如peek,add,poll等。
要求：可以使用现成的栈对象
设计思路：
栈的规则是先进后出，例如压进1,2,3;则出栈为3,2,1，
此时所有两个栈，则我们可以将两个栈分成压入栈（stackPush）和弹出栈（stackPop）。
1.当有数据执行add操作时，则往stackPush压入一个数据。
2.当执行peek和poll操纵时，判断stackPop是否为空，
   如果不为空,则直接查询或弹出栈栈顶元素。
   如果为空,并且stackPush也不为空,则将stackpush的元素一个个弹出，并压入stackPop（此操作类似于将所剩元素进行顺序颠倒）

注意事项:
    为什么不每次进行peek和poll操作时，都进行顺序颠倒。举个例子说明：
    1.压入元素1，2，3
    2.执行peek操作
    3.压入元素4，5
    4,执行peek操作
    此时顺序为4.5，1，2，3。显然顺序不对
    原因：因为第一次在peek操作时，将stackPush数据调换顺序，并放入stackPop，此时顺序是对的，没问题，第二次进行peek时，由于stackPop
    里的元素顺序是对的，而peek操作所复制过来的新数据会放在栈顶，但是原本数据才是最先进来的，导致整体顺序不对。

    关键点:只要我们保证stackPop里的数据是最先的几个且顺序对就行，当stackPop里没有数据了，我们再去stackPush里拿进来就行，而不是每次都拿，
