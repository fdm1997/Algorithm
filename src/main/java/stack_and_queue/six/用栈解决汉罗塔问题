问题：用栈解决汉罗塔问题。
规则，把所有盘从最左边移到最右边，不能直接从最左边移到最右边，移动过程中大的盘不能在上面。（盘用数字代替）
思路：
用三个栈模拟三个杆，初始数据全部在左杆上
1.先将左边栈顶元素移到中间。
2.将左边栈顶元素与中间栈顶元素做对比，
   若中间小，则将中间移到右边，如此循环，直到条件不成立，然后将左边移到中间，然后把右边依次移到中间。
   若中间大，则直接将左边元素移到中间。
3，重复第二步操作，直到左边栈为空，
4.将中间